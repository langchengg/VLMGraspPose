# Offline VGN inference dependencies.  ROS, pybullet, MPI and training packages
# from the upstream repository are intentionally excluded.
numpy>=1.24,<3
scipy>=1.10,<2
torch>=2.0
open3d>=0.18,<0.20
Pillow>=10
PyYAML>=6
matplotlib>=3.7
pytest>=8
