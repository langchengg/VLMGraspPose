# Local Datasets

This directory is for local datasets used by the active pipeline.

Expected local layout:

```text
data/
├── OCID-VLG/
└── OCID-Grasp/
```

The dataset contents are ignored by Git because they are large. Keep only this README and optional `.gitkeep` placeholders under version control.
