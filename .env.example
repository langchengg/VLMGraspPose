# Optional Hugging Face authentication for gated SAM3 assets.
HF_TOKEN=

# Optional local SAM3 integration-test model coordinates.
SAM3_MODEL_PATH=
SAM3_MODEL_REVISION=
RUN_SAM3_CPU_INTEGRATION=0
RUN_SAM3_GPU_TESTS=0

# Optional providers for literature tooling. Add real values only to an
# untracked local .env when a later workflow explicitly requires them.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
SEMANTIC_SCHOLAR_API_KEY=
CROSSREF_API_KEY=
